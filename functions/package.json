{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "contact.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild contact.js --bundle --platform=node --outdir=dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {

  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
